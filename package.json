{
  "name": "express-function-server",
  "version": "1.0.0",
  "description": "Express server that returns functions based on numbers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jest": "^30.2.0"
  }
}
